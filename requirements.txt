--index-url https://pypi.org/simple/

# Deep learning core
torch>=2.1.0
torchaudio>=2.1.0

# HuggingFace ecosystem
huggingface_hub>=0.20.0
transformers>=4.40.0
accelerate>=0.25.0
sentencepiece>=0.2.0

# Fish Audio S2 Pro - installed from cloned repo
# The repo is cloned during setup and pip installed in editable mode

# MOSS-TTS - installed from cloned repo
# The repo is cloned during setup and pip installed in editable mode

# OmniVoice
omnivoice>=0.1.0

# Audio processing
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0
scipy>=1.11.0

# Utility
requests>=2.31.0
pyyaml>=6.0
tqdm>=4.66.0
einops>=0.7.0

# Fish-speech runtime deps
vector-quantize-pytorch>=1.14.0
vocos>=0.1.0
funasr>=1.0.0
